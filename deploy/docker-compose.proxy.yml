# TLS reverse proxy overlay — use with HERMES_BEHIND_PROXY=1
services:
  caddy:
    image: caddy:2-alpine
    network_mode: host
    volumes:
      - ./caddy/Caddyfile.local:/etc/caddy/Caddyfile:ro
      - caddy-data:/data
      - caddy-config:/config

volumes:
  caddy-data:
  caddy-config:
